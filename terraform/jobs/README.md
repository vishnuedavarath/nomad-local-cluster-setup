# Terraform Jobs Workspace

This directory has been split into separate Terraform workspaces:

- `../operations` for cluster-side operational resources such as namespaces, ACLs, secrets, and host volumes
- `../development` for sample and development workloads such as the nginx job

Use those directories directly for new applies.
